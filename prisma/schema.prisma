datasource db {
  provider = "mysql"
  url = env("DATABASE_URL") // uses connection pooling
}

generator client {
  provider        = "prisma-client-js"
}

model Dictionary {
  id         String   @id @default(cuid())
  word      String
  length    Int
}